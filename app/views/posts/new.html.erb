<h1>New Post</h1>

<%= simple_form_for @post, url: create_post_path do |f| %>
	<div class="form-group">
		<%= f.input :title %>
		<%= f.input :content, as: :text %>
		<%= f.submit "Submit", class: 'btn btn-primary btn-lg' %>
	</div>
<% end %>